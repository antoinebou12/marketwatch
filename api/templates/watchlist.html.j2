<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>My Watchlist</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900">
    <div class="container mx-auto px-4 py-8">
        <div class="bg-gray-800 rounded-lg shadow-lg overflow-hidden">
            <h1 class="text-3xl font-bold text-white bg-gray-900 px-4 py-2">My Watchlist</h1>
            <table class="table-auto w-full mt-8">
                <thead>
                    <tr>
                        <th class="px-4 py-2 text-white">Company</th>
                        <th class="px-4 py-2 text-white">Price</th>
                        <th class="px-4 py-2 text-white">Change</th>
                        <th class="px-4 py-2 text-white">% Change</th>
                        <th class="px-4 py-2 text-white">Volume</th>
                        <th class="px-4 py-2 text-white">Avg Volume</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in data %}
                        <tr>
                            <td class="border px-4 py-2">{{ item.companyName }}</td>
                            <td class="border px-4 py-2">{{ item.quote.liveData.price }}</td>
                            <td class="border px-4 py-2">{{ item.quote.liveData.change }}</td>
                            <td class="border px-4 py-2">{{ item.quote.liveData.percentChange }}</td>
                            <td class="border px-4 py-2">{{ item.quote.liveData.volume }}</td>
                            <td class="border px-4 py-2">{{ item.quote.liveData.averageVolume }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
